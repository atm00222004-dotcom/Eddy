<Page x:Class="ConfigurationKeyGenerator.Views.LicenseList"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      mc:Ignorable="d"
      Background="#F3F4F6"
      FontFamily="Segoe UI">

    <Page.Resources>

        <!-- Primary action button -->
        <Style x:Key="PrimaryButton" TargetType="Button">
            <Setter Property="Background" Value="#2563EB"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="Width" Value="190"/>
            <Setter Property="Height" Value="40"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="6">
                            <ContentPresenter HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#1D4ED8"/>
                </Trigger>
                <Trigger Property="IsPressed" Value="True">
                    <Setter Property="Background" Value="#1E40AF"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Small action button used inside the grid rows -->
        <Style x:Key="RowActionButton" TargetType="Button">
            <Setter Property="Background" Value="#EFF6FF"/>
            <Setter Property="Foreground" Value="#2563EB"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Width" Value="100"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <StackPanel Orientation="Horizontal"
                                        HorizontalAlignment="Center"
                                        VerticalAlignment="Center">
                                <TextBlock Text="&#8595;"
                                           FontWeight="Bold"
                                           VerticalAlignment="Center"
                                           Margin="0,0,5,0"/>
                                <ContentPresenter VerticalAlignment="Center"/>
                            </StackPanel>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#DBEAFE"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Secondary action button (Edit) used inside the grid rows, aligned to match RowActionButton -->
        <Style x:Key="RowSecondaryButton" TargetType="Button">
            <Setter Property="Background" Value="#F3F4F6"/>
            <Setter Property="Foreground" Value="#374151"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12.5"/>
            <Setter Property="Width" Value="70"/>
            <Setter Property="Height" Value="30"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                CornerRadius="5">
                            <ContentPresenter HorizontalAlignment="Center"
                                               VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#E5E7EB"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Search box -->
        <Style x:Key="SearchBox" TargetType="TextBox">
            <Setter Property="Height" Value="38"/>
            <Setter Property="Width" Value="260"/>
            <Setter Property="Padding" Value="34,0,10,0"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderBrush" Value="#D1D5DB"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="Background" Value="White"/>
        </Style>

        <!-- DataGrid column header -->
        <Style TargetType="DataGridColumnHeader">
            <Setter Property="Background" Value="#F9FAFB"/>
            <Setter Property="Foreground" Value="#6B7280"/>
            <Setter Property="FontWeight" Value="SemiBold"/>
            <Setter Property="FontSize" Value="12"/>
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="HorizontalContentAlignment" Value="Left"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Setter Property="BorderBrush" Value="#E5E7EB"/>
        </Style>

        <!-- DataGrid cell -->
        <Style TargetType="DataGridCell">
            <Setter Property="Padding" Value="14,10"/>
            <Setter Property="BorderThickness" Value="0"/>
            <Setter Property="Foreground" Value="#1F2937"/>
            <Setter Property="FontSize" Value="13"/>
            <Setter Property="VerticalContentAlignment" Value="Center"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="DataGridCell">
                        <Border Background="{TemplateBinding Background}"
                                Padding="{TemplateBinding Padding}"
                                VerticalAlignment="Center">
                            <ContentPresenter VerticalAlignment="Center"/>
                        </Border>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
            <Style.Triggers>
                <Trigger Property="IsSelected" Value="True">
                    <Setter Property="Background" Value="#EFF6FF"/>
                    <Setter Property="Foreground" Value="#1F2937"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- DataGrid row -->
        <Style TargetType="DataGridRow">
            <Setter Property="Background" Value="White"/>
            <Setter Property="BorderBrush" Value="#F3F4F6"/>
            <Setter Property="BorderThickness" Value="0,0,0,1"/>
            <Style.Triggers>
                <Trigger Property="IsMouseOver" Value="True">
                    <Setter Property="Background" Value="#F9FAFB"/>
                </Trigger>
                <Trigger Property="AlternationIndex" Value="1">
                    <Setter Property="Background" Value="#FCFCFD"/>
                </Trigger>
            </Style.Triggers>
        </Style>

        <!-- Style for the Machine ID text so a long hash doesn't blow out the row -->
        <Style x:Key="MachineIdText" TargetType="TextBlock">
            <Setter Property="FontFamily" Value="Consolas"/>
            <Setter Property="FontSize" Value="11.5"/>
            <Setter Property="Foreground" Value="#4B5563"/>
            <Setter Property="TextTrimming" Value="CharacterEllipsis"/>
            <Setter Property="VerticalAlignment" Value="Center"/>
            <Setter Property="ToolTip" Value="{Binding MachineId}"/>
        </Style>

    </Page.Resources>

    <Grid Margin="40,30,40,30">

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <!-- Header -->
        <Grid VerticalAlignment="Center">

            <Grid.ColumnDefinitions>
                <ColumnDefinition/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>

            <StackPanel Orientation="Horizontal" VerticalAlignment="Center">

                <!-- Logo mark -->
                <Border Width="48"
                        Height="48"
                        CornerRadius="10"
                        VerticalAlignment="Center"
                        Margin="0,0,14,0"
                        ClipToBounds="True">
                    <Image Source="/Assets/Magkraft.jpg"
                           Width="48"
                           Height="48"
                           Stretch="UniformToFill"/>
                </Border>

                <StackPanel VerticalAlignment="Center">

                    <TextBlock Text="Configuration Management"
                               FontSize="26"
                               FontWeight="SemiBold"
                               Foreground="#111827"/>

                    <TextBlock Text="Configuration Key History &#8226; View and download previously generated configurations"
                               FontSize="13"
                               Foreground="Gray"
                               Margin="0,5,0,0"/>

                </StackPanel>

            </StackPanel>

            <Button Grid.Column="1"
                    Content="+ Add Configuration"
                    Style="{StaticResource PrimaryButton}"
                    HorizontalAlignment="Right"
                    VerticalAlignment="Center"
                    Click="AddLicense_Click"/>

        </Grid>

        <!-- Toolbar: search + count -->
        <Grid Grid.Row="1" Margin="0,24,0,0" VerticalAlignment="Center">

            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <Grid VerticalAlignment="Center">
                <TextBox x:Name="txtSearch"
                         Style="{StaticResource SearchBox}"
                         VerticalAlignment="Center"
                         TextChanged="TxtSearch_TextChanged"/>

                <TextBlock Text="&#128269;"
                           FontSize="13"
                           Foreground="#9CA3AF"
                           Margin="10,0,0,0"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"/>

                <TextBlock x:Name="txtSearchPlaceholder"
                           Text="Search by customer, machine ID..."
                           FontSize="12.5"
                           Foreground="#9CA3AF"
                           Margin="34,0,0,0"
                           HorizontalAlignment="Left"
                           VerticalAlignment="Center"
                           IsHitTestVisible="False"/>
            </Grid>

        </Grid>

        <!-- Grid -->
        <Border Grid.Row="2"
                Margin="0,20,0,0"
                Background="White"
                CornerRadius="10"
                BorderBrush="#E5E7EB"
                BorderThickness="1"
                Padding="0">

            <Border.Effect>
                <DropShadowEffect BlurRadius="18"
                                  ShadowDepth="2"
                                  Opacity="0.06"
                                  Color="Black"/>
            </Border.Effect>

            <Grid>

                <DataGrid x:Name="licenseGrid"
                          AutoGenerateColumns="False"
                          IsReadOnly="True"
                          CanUserAddRows="False"
                          CanUserResizeRows="False"
                          CanUserResizeColumns="False"
                          CanUserReorderColumns="False"
                          ColumnWidth="SizeToCells"
                          GridLinesVisibility="None"
                          HeadersVisibility="Column"
                          RowHeight="52"
                          AlternationCount="2"
                          BorderThickness="0"
                          Background="Transparent"
                          RowBackground="White"
                          VerticalGridLinesBrush="Transparent"
                          HorizontalGridLinesBrush="Transparent">

                    <DataGrid.Columns>

                        <DataGridTextColumn Header="PRODUCT"
                                            Binding="{Binding ProductName}"
                                            Width="130"/>

                        <DataGridTextColumn Header="CUSTOMER NAME"
                                            Binding="{Binding CustomerName}"
                                            Width="170"/>

                        <!-- Machine ID: fills remaining space so the row uses the full grid width -->
                        <DataGridTemplateColumn Header="MACHINE ID"
                                                 Width="*"
                                                 MinWidth="260">
                            <DataGridTemplateColumn.CellTemplate>
                                <DataTemplate>
                                    <TextBlock Text="{Binding MachineId}"
                                               Style="{StaticResource MachineIdText}"/>
                                </DataTemplate>
                            </DataGridTemplateColumn.CellTemplate>
                        </DataGridTemplateColumn>

                        <DataGridTextColumn Header="GENERATED DATE"
                                            Binding="{Binding GeneratedDate, StringFormat={}{0:dd-MM-yyyy HH:mm}}"
                                            Width="165"/>

                        <DataGridTextColumn Header="UPDATED DATE"
                                            Width="150">
                            <DataGridTextColumn.Binding>
                                <Binding Path="UpdatedDate"
                                         TargetNullValue="Not Updated"
                                         StringFormat="{}{0:dd-MM-yyyy HH:mm}" />
                            </DataGridTextColumn.Binding>
                        </DataGridTextColumn>

                        <DataGridTemplateColumn Header="ACTION"
                                                 Width="210">

                            <DataGridTemplateColumn.CellTemplate>

                                <DataTemplate>

                                    <StackPanel Orientation="Horizontal"
                                                HorizontalAlignment="Left"
                                                VerticalAlignment="Center">

                                        <Button Content="Edit"
                                                Style="{StaticResource RowSecondaryButton}"
                                                Margin="0,0,8,0"
                                                VerticalAlignment="Center"
                                                Click="Edit_Click"/>

                                        <Button Content="Download"
                                                Style="{StaticResource RowActionButton}"
                                                VerticalAlignment="Center"
                                                Click="Download_Click"/>

                                    </StackPanel>

                                </DataTemplate>

                            </DataGridTemplateColumn.CellTemplate>

                        </DataGridTemplateColumn>

                    </DataGrid.Columns>

                </DataGrid>

                <!-- Empty state, shown when there are no records -->
                <StackPanel x:Name="panelEmptyState"
                            HorizontalAlignment="Center"
                            VerticalAlignment="Center"
                            Visibility="Collapsed"
                            IsHitTestVisible="False">

                    <TextBlock Text="&#128193;"
                               FontSize="34"
                               HorizontalAlignment="Center"
                               Margin="0,0,0,10"/>

                    <TextBlock x:Name="txtEmptyTitle"
                               Text="No configuration yet"
                               FontSize="15"
                               FontWeight="SemiBold"
                               Foreground="#374151"
                               HorizontalAlignment="Center"/>

                    <TextBlock x:Name="txtEmptySubtitle"
                               Text="Generated licenses will appear here."
                               FontSize="12.5"
                               Foreground="#9CA3AF"
                               HorizontalAlignment="Center"
                               Margin="0,4,0,0"/>

                </StackPanel>

            </Grid>

        </Border>

    </Grid>

</Page>
