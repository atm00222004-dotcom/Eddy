<Window x:Class="_8F.ExportProfilePickerWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Export Configuration Profile" Height="480" Width="720"
        WindowStartupLocation="CenterScreen" ResizeMode="NoResize"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True" Loaded="Window_Loaded">

    <Window.Resources>
        <Style x:Key="MagkraftButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#305371"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#FF000000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="10">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsEnabled" Value="False">
                                <Setter Property="Background" Value="#94A3B8"/>
                                <Setter Property="Foreground" Value="#E2E8F0"/>
                                <Setter Property="BorderBrush" Value="#CBD5E1"/>
                            </Trigger>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E3A8A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MagkraftSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource MagkraftButtonStyle}">
            <Setter Property="Background" Value="#64748B"/>
        </Style>
    </Window.Resources>

    <Border Name="brRef" Margin="4" Background="#DBDBDB" BorderBrush="#B0B0B0" BorderThickness="1" CornerRadius="20" MouseDown="Window_MouseDown">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/> <!-- Header Title -->
                <RowDefinition Height="Auto"/> <!-- Separator -->
                <RowDefinition Height="*"/>    <!-- DataGrid Profiles -->
                <RowDefinition Height="Auto"/> <!-- Status Notice -->
                <RowDefinition Height="Auto"/> <!-- Action Buttons -->
            </Grid.RowDefinitions>

            <!-- 1. Header Title -->
            <Grid Grid.Row="0" Margin="0,4,0,8">
                <Label Content="Export Saved Configuration Profile" FontSize="20" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center" VerticalAlignment="Center"/>
                <Button Name="btnCloseHeader" Content="Close" Width="70" Height="30" Style="{StaticResource MagkraftSecondaryButtonStyle}" FontSize="13" Click="btnCancel_Click" HorizontalAlignment="Right" VerticalAlignment="Center"/>
            </Grid>

            <Separator Grid.Row="1" Margin="0,0,0,10" Background="#A0A0A0"/>

            <!-- 2. DataGrid Container -->
            <Border Grid.Row="2" Background="#FFFFFF" BorderBrush="#A0A0A0" BorderThickness="1" CornerRadius="10" Padding="6" Margin="4,0,4,10">
                <DataGrid Name="dgProfiles" AutoGenerateColumns="False" IsReadOnly="True" SelectionMode="Single"
                          Background="#FFFFFF" RowBackground="#FFFFFF" AlternatingRowBackground="#F8FAFC"
                          Foreground="#0F172A" GridLinesVisibility="All" HorizontalGridLinesBrush="#CBD5E1" VerticalGridLinesBrush="#CBD5E1"
                          HeadersVisibility="Column" FontSize="13" ColumnHeaderHeight="32" RowHeight="30"
                          MouseDoubleClick="dgProfiles_MouseDoubleClick">
                    <DataGrid.Resources>
                        <Style TargetType="DataGridColumnHeader">
                            <Setter Property="Background" Value="#305371"/>
                            <Setter Property="Foreground" Value="White"/>
                            <Setter Property="FontWeight" Value="Bold"/>
                            <Setter Property="FontSize" Value="13"/>
                            <Setter Property="Padding" Value="6,4"/>
                            <Setter Property="HorizontalContentAlignment" Value="Center"/>
                        </Style>
                        <Style TargetType="DataGridCell">
                            <Setter Property="Padding" Value="4,2"/>
                            <Setter Property="VerticalAlignment" Value="Center"/>
                            <Setter Property="TextBlock.TextAlignment" Value="Center"/>
                        </Style>
                    </DataGrid.Resources>
                    <DataGrid.Columns>
                        <DataGridTextColumn Header="ID" Binding="{Binding Id}" Width="60"/>
                        <DataGridTextColumn Header="Profile Name" Binding="{Binding Name}" Width="*"/>
                        <DataGridTextColumn Header="Operator" Binding="{Binding OperatorName}" Width="150"/>
                        <DataGridTextColumn Header="Created Date/Time" Binding="{Binding CreatedAt, StringFormat=\{0:yyyy-MM-dd HH:mm:ss\}}" Width="180"/>
                    </DataGrid.Columns>
                </DataGrid>
            </Border>

            <!-- 3. Status Bar Notice -->
            <Border Grid.Row="3" Background="#E2E8F0" BorderBrush="#CBD5E1" BorderThickness="1" CornerRadius="6" Padding="8,4" Margin="4,0,4,10">
                <TextBlock Name="lblStatus" Text="Select a profile and click Export." Foreground="#0F172A" FontSize="13" FontWeight="SemiBold"/>
            </Border>

            <!-- 4. Action Buttons -->
            <Grid Grid.Row="4" Margin="4,0,4,4">
                <StackPanel Orientation="Horizontal" HorizontalAlignment="Right">
                    <Button Name="btnExport" Content="Export Profile" Width="150" Height="36" Style="{StaticResource MagkraftButtonStyle}" FontSize="14" Click="btnExport_Click" Margin="0,0,10,0"/>
                    <Button Name="btnCancel" Content="Cancel" Width="100" Height="36" Style="{StaticResource MagkraftSecondaryButtonStyle}" FontSize="14" Click="btnCancel_Click"/>
                </StackPanel>
            </Grid>
        </Grid>
    </Border>
</Window>
