<Window x:Class="_8F.PasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        mc:Ignorable="d"
        Title="Password Required" Height="240" Width="420"
        WindowStartupLocation="CenterOwner" ResizeMode="NoResize"
        WindowStyle="None" Background="Transparent" AllowsTransparency="True">

    <Window.Resources>
        <Style x:Key="MagkraftButtonStyle" TargetType="Button">
            <Setter Property="Background" Value="#305371"/>
            <Setter Property="Foreground" Value="White"/>
            <Setter Property="FontWeight" Value="Bold"/>
            <Setter Property="FontSize" Value="14"/>
            <Setter Property="BorderBrush" Value="#FF000000"/>
            <Setter Property="BorderThickness" Value="1"/>
            <Setter Property="Cursor" Value="Hand"/>
            <Setter Property="Template">
                <Setter.Value>
                    <ControlTemplate TargetType="Button">
                        <Border Background="{TemplateBinding Background}"
                                BorderBrush="{TemplateBinding BorderBrush}"
                                BorderThickness="{TemplateBinding BorderThickness}"
                                CornerRadius="8">
                            <ContentPresenter HorizontalAlignment="Center" VerticalAlignment="Center"/>
                        </Border>
                        <ControlTemplate.Triggers>
                            <Trigger Property="IsMouseOver" Value="True">
                                <Setter Property="Background" Value="#1E3A8A"/>
                            </Trigger>
                        </ControlTemplate.Triggers>
                    </ControlTemplate>
                </Setter.Value>
            </Setter>
        </Style>

        <Style x:Key="MagkraftSecondaryButtonStyle" TargetType="Button" BasedOn="{StaticResource MagkraftButtonStyle}">
            <Setter Property="Background" Value="#64748B"/>
        </Style>
    </Window.Resources>

    <Border Margin="4" Background="#DBDBDB" BorderBrush="#B0B0B0" BorderThickness="1" CornerRadius="16" MouseDown="Window_MouseDown">
        <Grid Margin="16">
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="*"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <!-- Header Title -->
            <Label Grid.Row="0" Content="Password Required" FontSize="18" FontWeight="Bold" Foreground="#0F172A" HorizontalAlignment="Center"/>

            <Separator Grid.Row="1" Margin="0,4,0,12" Background="#A0A0A0"/>

            <!-- Instruction -->
            <TextBlock Grid.Row="2" Text="Enter password to access Configuration menu items:" Foreground="#334155" FontSize="13" FontWeight="SemiBold" Margin="4,0,4,8" TextWrapping="Wrap"/>

            <!-- Password Field -->
            <PasswordBox Grid.Row="3" Name="txtPassword" Height="36" FontSize="16" VerticalContentAlignment="Center" Padding="6,0" Margin="4,0,4,12" KeyDown="txtPassword_KeyDown"/>

            <!-- Action Buttons -->
            <StackPanel Grid.Row="4" Orientation="Horizontal" HorizontalAlignment="Right" Margin="4,0,4,0">
                <Button Name="btnOK" Content="OK" Width="90" Height="34" Style="{StaticResource MagkraftButtonStyle}" IsDefault="True" Click="btnOK_Click" Margin="0,0,10,0"/>
                <Button Name="btnCancel" Content="Cancel" Width="90" Height="34" Style="{StaticResource MagkraftSecondaryButtonStyle}" IsCancel="True" Click="btnCancel_Click"/>
            </StackPanel>
        </Grid>
    </Border>
</Window>
